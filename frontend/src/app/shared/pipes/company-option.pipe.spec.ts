import { CompanyOptionPipe } from './company-option.pipe';

describe('CompanyOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
