import { PaginationInfoPipe } from './pagination-info.pipe';

describe('PaginationInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginationInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
